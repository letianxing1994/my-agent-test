# Agent 预览系统启动指南

## 问题分析

### 问题1：Planning Agent 未连接
**原因**：Planning Agent 服务没有启动，导致 A2A Server 找不到可用的 Planning Agent。

**日志特征**：
```
[A2A Server] 📊 当前活跃Agent数量: 0
[A2A Server] 📋 活跃Agent列表: 无
[A2A Server] 🔍 查找planning-agent: 未找到
[A2A Server] ⚠️ Planning Agent 未连接或状态不正常，任务等待中
```

### 问题2：前端持续轮询导致429错误和网站崩溃
**原因**：
1. 前端使用2秒间隔的轮询，频率过高
2. 没有SSE支持，只能依赖轮询
3. 遇到429错误时继续轮询，加剧问题，最终导致网站崩溃

**已修复 - 完全移除轮询**：
- ✅ 添加SSE实时推送支持
- ✅ **完全移除轮询机制**（避免网站崩溃）
- ✅ 页面加载时只调用一次API获取初始状态
- ✅ 之后完全依赖SSE实时推送更新
- ✅ SSE失败时显示警告，用户需手动刷新
- ✅ 添加"刷新状态"按钮，用户主动控制

## 正确启动步骤

### 1. 启动 A2A Server
```bash
cd E:\NodeProject\my-agent-test
npm run start:a2a-server
```

**预期日志**：
```
A2A服务器启动在 http://localhost:8080
WebSocket服务器就绪
```

### 2. 启动 Planning Agent（⚠️ 必须步骤）
```bash
cd E:\NodeProject\my-agent-test
npm run start:planning-agent
```

**预期日志**：
```
============================================
🚀 [Planning Agent] 正在启动...
============================================

[ReAct Planning Agent] 已连接到A2A服务器
[ReAct Planning Agent] 🔄 正在注册到A2A服务器，Agent ID: planning-agent
[ReAct Planning Agent] ✅ 注册消息已发送到A2A服务器 (MessageType: STATUS_UPDATE, Action: register)
```

**A2A Server 应该显示**：
```
[A2A Server] ✅ Agent planning-agent 已注册成功
[A2A Server] 📊 当前活跃Agent列表: planning-agent
```

### 3. 启动 game-factory Backend
```bash
cd E:\NodeProject\game-factory\backend
npm start
```

### 4. 启动 game-factory Frontend
```bash
cd E:\NodeProject\game-factory\frontend
npm run dev
```

## 验证系统正常工作

### 检查1：Planning Agent 连接状态
在 A2A Server 的控制台中，当你点击"试运行"时应该看到：

```
[A2A Server] 🎯 准备发送策划任务，ProjectID: preview-xxx
[A2A Server] 📊 当前活跃Agent数量: 1
[A2A Server] 📋 活跃Agent列表: planning-agent
[A2A Server] 🔍 查找planning-agent: 找到
[A2A Server] 🔌 WebSocket状态: OPEN
[A2A Server] ✅ 已发送策划任务到 Planning Agent: preview-xxx
[A2A Server] 📤 消息类型: USER_INPUT, MessageID: xxx
```

在 Planning Agent 的控制台中应该看到：

```
[ReAct Planning Agent] 📨 收到消息: USER_INPUT 来自: a2a-server, ProjectID: preview-xxx
[ReAct Planning Agent] 🎯 收到USER_INPUT任务，准备执行ReAct循环
```

### 检查2：前端状态更新方式

**正常情况（SSE工作）**：
- 页面加载时调用一次API获取初始状态
- 之后通过SSE实时接收状态更新
- 浏览器Network标签中只有1次API请求 + 1个持续的SSE连接
- **没有轮询请求**

**SSE失败情况**：
- 页面顶部显示黄色警告："实时更新连接失败"
- 用户需要点击右上角"刷新状态"按钮手动获取最新状态
- **不会自动轮询**，避免429错误和网站崩溃

浏览器控制台应该看到：
```
[PreviewTaskDetail] 尝试订阅SSE实时更新
```

如果SSE失败：
```
[PreviewTaskDetail] SSE连接失败，请手动刷新页面获取最新状态
```

## 常见问题排查

### 问题：Planning Agent 启动后没有连接
1. 检查 A2A Server 是否已启动
2. 检查端口8080是否被占用
3. 检查 `.env` 文件中的 `A2A_SERVER_URL` 配置

### 问题：SSE连接失败，显示警告
1. 检查 game-factory backend 是否正常运行
2. 检查 my-agent-test 的 A2A Server 是否正常运行
3. 点击页面右上角"刷新状态"按钮手动获取最新状态
4. **重要**：不会自动轮询，避免429错误

### 问题：任务状态不更新
1. 检查是否显示SSE连接失败警告
2. 如果有警告，点击"刷新状态"按钮
3. 刷新浏览器页面重新建立SSE连接
4. **不要担心**：不会因为自动轮询导致网站崩溃

## 架构说明

```
Frontend (浏览器)
    ↓
    ├─ 初始加载：调用1次API获取初始状态
    └─ 实时更新：SSE订阅 (不降级到轮询)
         ↓
game-factory Backend (SSE代理)
    ↓
my-agent-test A2A Server (SSE源)
    ↓
Planning Agent (通过WebSocket发送状态)
```

**状态更新流程**：
1. Planning Agent 执行任务，状态变化
2. 通过 TaskStateManager 触发事件
3. A2A Server 的 SSE 端点推送给所有订阅者
4. game-factory backend 转发给前端
5. 前端实时显示状态变化

**关键设计决策**：
- ✅ **完全移除轮询**：避免频繁请求导致429错误和网站崩溃
- ✅ **SSE失败不降级**：显示警告，让用户手动刷新
- ✅ **用户主动控制**：通过"刷新状态"按钮主动获取最新状态
- ✅ **一次性加载**：页面加载时只调用一次API

## 修改摘要

### my-agent-test
1. ✅ Planning Agent 注册日志增强
2. ✅ A2A Server 连接状态详细日志
3. ✅ sendPlanningTask 详细诊断信息

### game-factory backend
1. ✅ 添加 SSE 代理端点 `/api/preview-tasks/:taskId/events`

### game-factory frontend
1. ✅ 添加 `subscribePreviewTaskStatus()` SSE订阅函数
2. ✅ **完全移除轮询机制**，只保留手动刷新功能
3. ✅ `PreviewTaskDetail` 改为：
   - 页面加载时调用1次API获取初始状态
   - 订阅SSE接收实时更新
   - SSE失败时显示警告，不自动轮询
   - 添加"刷新状态"按钮供用户手动刷新
4. ✅ 添加SSE连接失败提示UI

## 优势

**相比之前的轮询方案**：
- 🚀 **性能提升**：从每2秒1次请求 → 0次请求（SSE推送）
- 🛡️ **避免崩溃**：不会因为轮询导致429错误和网站崩溃
- ⚡ **实时性更好**：状态变化立即推送，无延迟
- 💡 **用户友好**：失败时明确提示，用户主动控制刷新
- 📊 **可观测性**：清晰的日志追踪整个连接流程

## WebSocket 连接诊断

### 最新增强的诊断日志

为了诊断 Planning Agent 未连接的问题，已添加详细的 WebSocket 通信日志。

### Planning Agent 预期日志

**成功连接时应该看到**：
```
[ReAct Planning Agent] 🔄 正在连接到 A2A 服务器: ws://localhost:8080
[ReAct Planning Agent] ✅ WebSocket连接已建立
[ReAct Planning Agent] 📡 WebSocket readyState: 1 (1=OPEN)
[ReAct Planning Agent] 🔄 正在注册到A2A服务器，Agent ID: planning-agent
[ReAct Planning Agent] ✅ 注册消息已发送到A2A服务器 (MessageType: STATUS_UPDATE, Action: register)
```

**接收到A2A Server确认时**：
```
[ReAct Planning Agent] 📨 收到消息（前100字符）: {"messageId":"xxx","senderId":"a2a-server",...
[ReAct Planning Agent] 📨 收到消息: STATUS_UPDATE 来自: a2a-server, ProjectID: N/A
```

**接收到任务时**：
```
[ReAct Planning Agent] 📨 收到消息: USER_INPUT 来自: a2a-server, ProjectID: preview-xxx
[ReAct Planning Agent] 🎯 收到USER_INPUT任务，准备执行ReAct循环
```

### A2A Server 预期日志

**接收到 Planning Agent 连接时**：
```
[A2A Server] 🔌 新的WebSocket连接已建立
[A2A Server] 📨 收到WebSocket原始消息: {"messageId":"xxx","senderId":"planning-agent",...
[A2A Server] 📦 解析后的消息类型: STATUS_UPDATE, 发送者: planning-agent, 接收者: a2a-server
[A2A Server] 🎯 检测到注册消息！准备注册 Agent: planning-agent
[A2A Server] ✅ Agent planning-agent 已注册成功
[A2A Server] 📊 当前活跃Agent列表: planning-agent
[A2A Server] 📤 已向 planning-agent 发送注册确认
```

**发送任务时**：
```
[A2A Server] 🎯 准备发送策划任务，ProjectID: preview-xxx
[A2A Server] 📊 当前活跃Agent数量: 1
[A2A Server] 📋 活跃Agent列表: planning-agent
[A2A Server] 🔍 查找planning-agent: 找到
[A2A Server] 🔌 WebSocket状态: OPEN
[A2A Server] ✅ 已发送策划任务到 Planning Agent: preview-xxx
[A2A Server] 📤 消息类型: USER_INPUT, MessageID: xxx
```

### 问题诊断步骤

**如果 Planning Agent 显示已发送注册消息，但 A2A Server 显示 0 个活跃Agent**：

1. **检查 A2A Server 是否收到 WebSocket 连接**：
   - 应该看到：`[A2A Server] 🔌 新的WebSocket连接已建立`
   - 如果没有，说明 WebSocket 连接未建立，检查端口和网络

2. **检查 A2A Server 是否收到注册消息**：
   - 应该看到：`[A2A Server] 📨 收到WebSocket原始消息: {"messageId"...`
   - 如果没有，说明消息未到达 A2A Server

3. **检查消息是否被正确解析**：
   - 应该看到：`[A2A Server] 📦 解析后的消息类型: STATUS_UPDATE`
   - 如果看到但没有后续注册成功消息，可能是消息格式问题

4. **检查注册是否被识别**：
   - 应该看到：`[A2A Server] 🎯 检测到注册消息！准备注册 Agent: planning-agent`
   - 如果没有，说明 `action: "register"` 字段可能有问题

5. **检查 WebSocket readyState**：
   - Planning Agent 在发送消息前应该显示 `readyState: 1 (1=OPEN)`
   - 如果不是 1，说明连接未完全建立

### 启动顺序很重要

**正确的启动顺序**：
1. **先启动 A2A Server**（等待 WebSocket 服务器就绪）
2. **再启动 Planning Agent**（连接到 A2A Server）
3. 启动 game-factory backend
4. 启动 game-factory frontend

**错误的启动顺序**：
- 如果先启动 Planning Agent，它会尝试连接到尚未启动的 A2A Server
- Planning Agent 会自动重连（每5秒），但最好按正确顺序启动避免混淆

### 下一步行动

1. **停止所有服务**：
   ```bash
   # 按 Ctrl+C 停止所有运行中的服务
   ```

2. **按正确顺序重启**：
   ```bash
   # 终端1: 启动 A2A Server
   cd E:\NodeProject\my-agent-test
   npm run start:a2a-server

   # 等待看到: "A2A服务器启动在 http://localhost:8080"

   # 终端2: 启动 Planning Agent
   npm run start:planning-agent

   # 等待看到: "[ReAct Planning Agent] ✅ 注册消息已发送到A2A服务器"
   # 并在终端1看到: "[A2A Server] ✅ Agent planning-agent 已注册成功"
   ```

3. **观察日志输出**：
   - 对比上面的"预期日志"，找出哪一步没有出现
   - 这将精确定位问题所在

4. **如果仍有问题**：
   - 将 A2A Server 和 Planning Agent 的完整启动日志保存
   - 特别注意任何错误或警告信息
   - 检查 WebSocket 连接是否被防火墙阻止